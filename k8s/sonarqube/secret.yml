# Create this secret before applying the other manifests:
#   kubectl create secret generic sonarqube-db-secret \
#     --namespace sonarqube \
#     --from-literal=username=sonar \
#     --from-literal=password=<your-password>
#
# This file is a placeholder template. Do NOT commit real credentials.
apiVersion: v1
kind: Secret
metadata:
  name: sonarqube-db-secret
  namespace: sonarqube
type: Opaque
stringData:
  username: sonar
  password: CHANGE_ME
