apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# DEPRECATED: This file is kept for reference.
# Use the environment-specific overlays instead:
#   Dev:  kubectl apply -k infra/k8s/app/overlays/dev/
#   Prod: kubectl apply -k infra/k8s/app/overlays/prod/
#   Shared (OVMS, OCR, F-Droid): kubectl apply -k infra/k8s/app/shared/
#
# See infra/k8s/app/README.md for full deployment instructions.

namespace: realestate

resources:
  - overlays/prod
