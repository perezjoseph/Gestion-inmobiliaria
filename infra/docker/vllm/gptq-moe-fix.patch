--- a/usr/local/lib/python3.12/dist-packages/vllm/model_executor/layers/quantization/auto_round.py
+++ b/usr/local/lib/python3.12/dist-packages/vllm/model_executor/layers/quantization/auto_round.py
@@ -159,6 +159,20 @@ class AutoRoundConfig(QuantizationConfig):
 
     def get_quant_method(self, layer: torch.nn.Module, prefix: str):
         if prefix and self.extra_config:
+            # Check if this layer is marked as 16-bit (unquantized) in
+            # extra_config. This handles MoE gate layers in models like
+            # Qwen3-30B-A3B where gates are fp16 while experts are int4.
+            # We check both exact match and with "model." prefix to cover
+            # all naming conventions used during model init.
+            for layer_name in self.extra_config:
+                if layer_name == prefix or layer_name == f"model.{prefix}" or f"model.{layer_name}" == prefix:
+                    cfg = self.extra_config[layer_name]
+                    if isinstance(cfg, dict) and cfg.get("bits", self.weight_bits) >= 16:
+                        from vllm.model_executor.layers.linear import LinearBase
+                        from vllm.model_executor.layers.vocab_parallel_embedding import ParallelLMHead
+                        if isinstance(layer, (LinearBase, ParallelLMHead)):
+                            return UnquantizedLinearMethod()
+                        return None
             for layer_name in self.extra_config:
                 if (
                     layer_name == prefix or layer_name == f"model.{prefix}"
@@ -255,6 +269,15 @@ class AutoRoundConfig(QuantizationConfig):
 
     def apply_ipex_quant_layer(self, layer, prefix: str):
+        # Early exit for layers marked as 16-bit in extra_config.
+        # This prevents MoE gate layers from being quantized on XPU,
+        # which triggers UR_RESULT_ERROR_DEVICE_LOST on Level Zero.
+        if self.extra_config and prefix:
+            for key in self.extra_config:
+                if key == prefix or key == f"model.{prefix}" or f"model.{key}" == prefix:
+                    if isinstance(self.extra_config[key], dict) and self.extra_config[key].get("bits", self.weight_bits) >= 16:
+                        if isinstance(layer, (LinearBase, ParallelLMHead)):
+                            return UnquantizedLinearMethod()
+                        return None
         weight_bits, group_size, sym = self.get_layer_config(layer, prefix)
         if not self.check_quantized(weight_bits):
             if isinstance(layer, (LinearBase, ParallelLMHead)):
