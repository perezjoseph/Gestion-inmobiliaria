{
  "tasks": [
    {
      "type": "kiro",
      "label": "Git Diff Review",
      "preCommand": "git diff 2>&1",
      "prompt": "Review this diff before I commit:\n\n{{commandOutput}}",
      "autonomyMode": "supervised"
    },
    {
      "type": "kiro",
      "label": "Explain Module",
      "prompt": "Explain the {{moduleName}} module — its purpose, key functions, and how it fits into the project",
      "autonomyMode": "supervised"
    },
    {
      "type": "kiro",
      "label": "Deep Code Review",
      "preCommand": "git log --oneline -20 2>&1",
      "prompt": "Review my recent git changes and suggest improvements. Activate the expert skills (perf-optimizer, maintainability-reviewer, rust-engineer) to analyze in depth. Use tools to read the actual changed files, run cargo clippy, and check for: N+1 queries, unnecessary clones, function length >50 lines, blocking in async, orphaned tasks, f64 precision loss in monetary code, overly broad pub visibility, and frontend anti-patterns (missing timer cleanup, HashMap props without Rc, large html! blocks). Categorize findings by severity (High/Medium/Low).\n\nRecent commits:\n{{commandOutput}}",
      "autonomyMode": "supervised"
    },
    {
      "type": "kiro",
      "label": "Plan & Fix Issues",
      "prompt": "Create a comprehensive plan to fix the following issues, write it to .kiro/plans/, have it reviewed by a code-reviewer sub-agent, then implement all fixes using sub-agents (rust-coder for backend, general-task-execution for frontend). Run cargo clippy and getDiagnostics after each phase to verify.\n\nIssues to fix:\n{{issues}}",
      "autonomyMode": "autopilot"
    },
    {
      "type": "kiro",
      "label": "Diagnostics Sweep",
      "preCommand": "git diff --name-only HEAD~5 2>&1",
      "prompt": "Run getDiagnostics on all these recently changed files (use d:\\\\realestate\\\\ prefix with backslashes for the workspace path). If there are any errors or warnings, fix them. Do not stop until all diagnostics are clean.\n\nFiles:\n{{commandOutput}}",
      "autonomyMode": "autopilot"
    },
    {
      "type": "kiro",
      "label": "Backend Clippy Check",
      "preCommand": "cd d:\\realestate\\backend && cargo clippy --all-targets 2>&1",
      "prompt": "Review the clippy output. If there are any warnings or errors, fix them directly. If clean, confirm.\n\n{{commandOutput}}",
      "autonomyMode": "autopilot"
    },
    {
      "type": "kiro",
      "label": "Run Agent Evals",
      "prompt": "Run agent evals for {{agentName}}. Read the orchestrator instructions from .kiro/evals/scripts/run_eval.md, then load the test cases from .kiro/evals/cases/{{agentName}}.json and execute the eval loop: invoke the specialist agent for each case, grade outputs against assertions, check for contamination, aggregate, and generate a report.",
      "autonomyMode": "autopilot"
    }
  ],
  "reminders": [
    {
      "label": "Review today's changes",
      "prompt": "Review my recent git changes and suggest improvements",
      "intervalMinutes": 120,
      "enabled": true
    }
  ]
}