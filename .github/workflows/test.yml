name: Tests

on:
  workflow_call:
    inputs:
      is-fork-pr:
        type: boolean
        required: true
    outputs:
      test-backend-result:
        value: ${{ jobs.test-backend.result }}
      test-backend-error-log:
        value: ${{ jobs.test-backend.outputs.error_log }}
      test-backend-duration:
        value: ${{ jobs.test-backend.outputs.duration }}
      test-frontend-result:
        value: ${{ jobs.test-frontend.result }}
      test-frontend-error-log:
        value: ${{ jobs.test-frontend.outputs.error_log }}
      test-frontend-duration:
        value: ${{ jobs.test-frontend.outputs.duration }}

permissions: {}

jobs:
  test-backend:
    name: Backend Tests & Coverage
    runs-on: arc-runner-dind
    timeout-minutes: 30
    if: ${{ !inputs.is-fork-pr }}
    permissions:
      contents: read
    env:
      SERVER_PORT: "8080"
      CORS_ORIGIN: http://localhost:8080
    outputs:
      error_log: ${{ steps.collect-errors.outputs.error_log }}
      duration: ${{ steps.timing.outputs.duration }}
    # ARC k3s: services run as sidecar containers in the pod.
    # The postgres container is accessible at localhost within the pod network.
    services:
      postgres:
        image: postgres:16@sha256:5a65324fe84dc41709ff914e90b07f3e2f577073ed27bf917d4873aca0c9ec51
        env:
          POSTGRES_DB: realestate
          POSTGRES_USER: realestate
          POSTGRES_PASSWORD: realestate_dev_password
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U realestate"
          --health-interval 5s
          --health-timeout 3s
          --health-retries 10
          --health-start-period 15s
    steps:
      - name: Record start time
        id: start
        run: echo "time=$(date +%s)" >> $GITHUB_OUTPUT

      - name: Checkout repository
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          persist-credentials: false

      - name: Setup Rust toolchain
        id: rust-setup
        uses: ./.github/actions/rust-setup
        with:
          components: llvm-tools-preview

      - name: Mask database credentials
        run: |
          echo "::add-mask::realestate_dev_password"
          echo "::add-mask::realestate"

      - name: Set database URL
        run: echo "DATABASE_URL=postgresql://realestate:realestate_dev_password@localhost:5432/realestate" >> $GITHUB_ENV

      - name: Install PostgreSQL client
        run: |
          if ! command -v pg_isready >/dev/null 2>&1; then
            echo "pg_isready not found -- installing postgresql-client"
            sudo apt-get update -qq
            sudo apt-get install -y -qq --no-install-recommends postgresql-client
          fi

      - name: Wait for Postgres to be ready
        run: |
          for i in $(seq 1 30); do
            if pg_isready -h localhost -p 5432 -U realestate 2>/dev/null; then
              echo "Postgres is ready"
              exit 0
            fi
            echo "Waiting for Postgres... attempt $i/30"
            sleep 2
          done
          echo "::error::Postgres did not become ready in time"
          exit 1

      - name: Generate JWT secret for tests
        run: |
          JWT_SECRET=$(openssl rand -hex 32)
          echo "::add-mask::$JWT_SECRET"
          echo "JWT_SECRET=$JWT_SECRET" >> $GITHUB_ENV

      - name: Install cargo-tarpaulin
        id: install-tarpaulin
        run: |
          TARPAULIN_VERSION="0.32.7"
          TARPAULIN_SHA256="67a61b2b271488e2d0cb718ccec384d0f296703f3c810f80ea5d46484c5d463b"
          ARCHIVE="${RUNNER_TEMP}/cargo-tarpaulin.tar.gz"
          curl -sSfL --proto =https --connect-timeout 15 --max-time 120 -o "$ARCHIVE" \
            "https://github.com/xd009642/tarpaulin/releases/download/${TARPAULIN_VERSION}/cargo-tarpaulin-x86_64-unknown-linux-musl.tar.gz"
          echo "${TARPAULIN_SHA256}  ${ARCHIVE}" | sha256sum -c -
          mkdir -p "${HOME}/.cargo/bin"
          tar xzf "$ARCHIVE" --no-same-owner -C "${HOME}/.cargo/bin" cargo-tarpaulin
          rm -f "$ARCHIVE"
          chmod +x "${HOME}/.cargo/bin/cargo-tarpaulin"
          cargo tarpaulin --version

      - name: Run tests with coverage (tarpaulin)
        id: tests
        run: |
          mkdir -p coverage
          set +e
          cargo tarpaulin --locked -p realestate-backend \
            --engine llvm \
            --out lcov --output-dir coverage \
            --timeout 300 --skip-clean 2>&1 | tee "${RUNNER_TEMP}/test-backend-output.txt"
          exit ${PIPESTATUS[0]}

      - name: Verify Cargo.lock is up to date
        run: |
          if ! git diff --quiet Cargo.lock; then
            echo "::error::Cargo.lock is out of date. Run 'cargo update' locally and commit the changes."
            git diff Cargo.lock
            exit 1
          fi

      - name: Scan coverage output for secrets
        if: steps.tests.outcome == 'success'
        run: |
          if grep -rqiE '(password|secret|token|key)=' coverage/ 2>/dev/null; then
            echo "::warning::Potential secrets detected in coverage output"
          fi

      - name: Upload coverage artifact
        if: steps.tests.outcome == 'success'
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a
        with:
          name: backend-coverage
          path: coverage/lcov.info
          retention-days: 7

      - name: Write test summary
        if: always()
        run: |
          {
            echo "## Backend Test Results"
            echo "| Check | Status |"
            echo "|-------|--------|"
            echo "| Tests & Coverage (tarpaulin) | ${{ steps.tests.outcome == 'success' && '✅ Pass' || '❌ Fail' }} |"
          } >> "$GITHUB_STEP_SUMMARY"

      - name: Record duration
        id: timing
        if: always()
        uses: ./.github/actions/job-timing
        with:
          start-time: ${{ steps.start.outputs.time }}

      - name: Collect error logs
        id: collect-errors
        if: always() && steps.tests.outcome == 'failure'
        run: |
          if [ -s "${RUNNER_TEMP}/test-backend-output.txt" ]; then
            ERROR=$(grep -E '(FAILED|error\[|panicked|failures:)' "${RUNNER_TEMP}/test-backend-output.txt" | head -c 2000 || true)
            if [ -z "$ERROR" ]; then
              ERROR=$(tail -c 3000 "${RUNNER_TEMP}/test-backend-output.txt")
            fi
          else
            ERROR="Backend tests failed. No captured output."
          fi
          EOF=$(dd if=/dev/urandom bs=15 count=1 status=none | base64)
          echo "error_log<<$EOF" >> $GITHUB_OUTPUT
          echo "$ERROR" >> $GITHUB_OUTPUT
          echo "$EOF" >> $GITHUB_OUTPUT

  test-frontend:
    name: Frontend WASM Compilation & Tests
    runs-on: arc-runner
    timeout-minutes: 20
    if: ${{ !inputs.is-fork-pr }}
    permissions:
      contents: read
    outputs:
      error_log: ${{ steps.collect-errors.outputs.error_log }}
      duration: ${{ steps.timing.outputs.duration }}
    steps:
      - name: Record start time
        id: start
        run: echo "time=$(date +%s)" >> $GITHUB_OUTPUT

      - name: Checkout repository
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          persist-credentials: false

      - name: Setup Rust toolchain
        id: rust-setup
        uses: ./.github/actions/rust-setup
        with:
          targets: wasm32-unknown-unknown

      - name: Run frontend tests
        id: tests
        run: |
          set +e
          cargo test --locked -p realestate-frontend --all-targets 2>&1 | tee "${RUNNER_TEMP}/test-frontend-output.txt"
          exit ${PIPESTATUS[0]}

      - name: Write test summary
        if: always()
        run: |
          {
            echo "## Frontend Test Results"
            echo "| Check | Status |"
            echo "|-------|--------|"
            echo "| WASM Compilation & Tests | ${{ steps.tests.outcome == 'success' && '✅ Pass' || '❌ Fail' }} |"
          } >> "$GITHUB_STEP_SUMMARY"

      - name: Record duration
        id: timing
        if: always()
        uses: ./.github/actions/job-timing
        with:
          start-time: ${{ steps.start.outputs.time }}

      - name: Collect error logs
        id: collect-errors
        if: always() && steps.tests.outcome == 'failure'
        run: |
          if [ -s "${RUNNER_TEMP}/test-frontend-output.txt" ]; then
            ERROR=$(grep -E '(FAILED|error\[|panicked|failures:)' "${RUNNER_TEMP}/test-frontend-output.txt" | head -c 2000 || true)
            if [ -z "$ERROR" ]; then
              ERROR=$(tail -c 3000 "${RUNNER_TEMP}/test-frontend-output.txt")
            fi
          else
            ERROR="Frontend tests failed. No captured output."
          fi
          EOF=$(dd if=/dev/urandom bs=15 count=1 status=none | base64)
          echo "error_log<<$EOF" >> $GITHUB_OUTPUT
          echo "$ERROR" >> $GITHUB_OUTPUT
          echo "$EOF" >> $GITHUB_OUTPUT
