#[cfg(test)]
#[allow(clippy::module_inception)]
mod propiedades_tests {
    #[test]
    #[ignore]
    fn test_list_propiedades() {
        todo!("Test list propiedades — requires database")
    }

    #[test]
    #[ignore]
    fn test_create_propiedad() {
        todo!("Test create propiedad — requires database")
    }
}
