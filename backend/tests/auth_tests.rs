#[cfg(test)]
#[allow(clippy::module_inception)]
mod auth_tests {
    #[test]
    #[ignore]
    fn test_login() {
        todo!("Test login — requires database")
    }

    #[test]
    #[ignore]
    fn test_register() {
        todo!("Test register — requires database")
    }
}
