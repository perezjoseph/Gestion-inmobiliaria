#[cfg(test)]
#[allow(clippy::module_inception)]
mod documentos_tests {
    #[test]
    #[ignore]
    fn test_upload_stores_file_and_metadata() {
        todo!("Test upload — requires database")
    }

    #[test]
    #[ignore]
    fn test_listar_documentos() {
        todo!("Test listar — requires database")
    }

    #[test]
    #[ignore]
    fn test_upload_auth_required() {
        todo!("Test auth on upload endpoint — requires running server")
    }
}
