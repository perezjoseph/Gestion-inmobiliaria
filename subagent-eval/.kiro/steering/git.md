---
inclusion: always
---

# Git Workflow

- Push directly to `main`. Do not create feature branches unless explicitly asked.
- Skip `--no-verify` only when pre-commit hooks fail on files unrelated to the current change.
