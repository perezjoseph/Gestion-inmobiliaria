#![allow(
    clippy::unwrap_used,
    clippy::expect_used,
    clippy::panic,
    clippy::todo,
    clippy::ignore_without_reason,
    clippy::if_not_else
)]
#[cfg(test)]
mod integration_tests {
    #[test]
    #[ignore]
    fn test_navigation() {
        todo!("Test navigation — requires browser environment")
    }
}
